=TRIM((

													SUBSTITUTE(
												SUBSTITUTE(
											SUBSTITUTE(
										SUBSTITUTE(
									SUBSTITUTE(
								SUBSTITUTE(
							SUBSTITUTE(
						SUBSTITUTE(
					SUBSTITUTE(
				SUBSTITUTE(
			SUBSTITUTE(
		SUBSTITUTE(
	SUBSTITUTE(
SUBSTITUTE('DROP ZONE'!I2,"/","-")
	,"\","-")
		,"[","-")
			,"[","-")
				,":","-")
					,";","-")
						,"|","-")
							,"=","-")
								,"+","-")
									,"*","-")
										,"?","-")
											,"<","-")
												,">","-")
													,"@","-")

))





Active Directory user and group names can contain all Unicode characters except for the following characters:
/
\
[
]
:
;
|
=
+
*
?
<
>
"
@
